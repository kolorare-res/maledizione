const config = {
  layers: 3,           // Ridotto da 4 a 3 layer
  maxDepth: 0.4375,    // Nuova profondità (3.5 cm / 8 cm)
  openDuration: 1500,  // Durata dell'animazione di apertura (ms)
  staggerDelay: 140,   // Ritardo tra l'apertura di un layer e l'altro (ms)
  easing: "easeOutCubic"
};